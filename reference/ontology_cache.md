# ontology cache

A cached list of ontology terms obtained from the ontology lookup
service (OLS) for ontology terms specified for objects in
`structToolbox`.

## Usage

``` r
ontology_cache()
```

## Value

list of cached ontology terms

## See also

ontology

## Examples

``` r
cache = ontology_cache()
```
